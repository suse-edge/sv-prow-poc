postsubmits:
- name: inrepo-test-postsubmit
  always_run: true
  decorate: true
  spec:
    containers:
      - image: alpine
        command: ["echo"]
        args: ["Hello from inrepoconfig postsubmit job!"]
